apiVersion: v1
kind: Service
metadata:
  name: immich-ml
  namespace: immich
  labels:
    app.kubernetes.io/name: immich-ml
    homelab/stack: immich
spec:
  selector:
    app.kubernetes.io/name: immich-ml
  ports:
    - port: 3003
      targetPort: 3003
